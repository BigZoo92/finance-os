{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for Finance-OS desktop shell.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
