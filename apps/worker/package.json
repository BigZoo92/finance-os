{
  "name": "@finance-os/worker",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun src/index.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@finance-os/env": "workspace:*",
    "@finance-os/db": "workspace:*",
    "@finance-os/redis": "workspace:*",
    "dotenv": "^17.3.1",
    "zod": "^3.25.76"
  }
}
